{
  "name": "typescript",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "vitest",
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "vitest": "^1.0.4"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
